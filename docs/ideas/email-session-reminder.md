# Email me a reminder of this session

Raw capture — 2026-05-01.

- "it would be cool to have a remind me of this session by email."
- Triggered after long Secretariat design conversation: email-as-transport, Secretariat-as-orchestrator, bilateral attention contracts, viral handshake via email, install + MCP + any agent, BYOK / local LLM / Claude Code cognition pluggability, architectural invariants.
- Could be generic Claude Code feature: end of session, send myself an email with key decisions, open threads, next concrete step.
- Or specifically a Secretariat use case — Secretariat sends *itself* an envelope summarizing what we just decided. Signed. Queued for tomorrow morning. Recursively interesting: the project's own correspondence primitive used to remind the principal about progress on the project.
- Self-correspondence as a category. "Future-me" as a recipient with a `did:key` of its own? Or just a flag on the envelope?
- Adjacent: what shape should the reminder take? AG-deepening (gross → subtle)? Plan diff? Action items? Decisions log? Open questions?
- This particular session would email me: invariants committed to AGENTS.md, architecture doc updated with cognition + transport adapter rows, distribution model = local daemon + MCP + any agent, viral handshake protocol sketched, key recovery still open (A/B/C options).
- Questions:
  - Generic Claude Code feature (works for any project), or Secretariat-specific (uses its own envelope flow)?
  - Triggered manually (slash command at session end) or scheduled (`/schedule` agent that drafts + sends)?
  - Once Secretariat exists, should reminders go *through* it — stamped envelopes to self? "Self-correspondence" as a first-class use case.
  - How does it integrate with Things, the runway skill, ideas/pain capture already in this setup?
  - Channel — email, or is the right shape a Things task with a link to the session transcript?
- Don't shape yet.
