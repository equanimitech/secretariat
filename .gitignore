# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.*
jscpd-report

# Rust
target/
# Cargo.lock is intentionally tracked — this workspace ships binaries
# (sec, sec-relay, sec-mcp), so deterministic dep resolution matters for
# reproducible builds (Docker, Railway).

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.tauri-keys/
