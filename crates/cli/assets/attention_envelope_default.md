# Attention envelope

This is your published bound — what depths and urgencies you accept right now,
on what cadence. Sender scribes are expected to read this before composing.

Edit freely. Re-stamp with `sec stamp ~/.secretariat/attention-envelope.md`
after edits so the bounds carry your signature.

## Defaults

- **Depths accepted:** gross, subtle
- **Urgencies accepted:** soon, whenever (reserve `now` for emergencies via
  the override channel below)
- **Cadence:** weekday mornings, 09:00–10:00 local time
- **Override channel:** phone call

## Notes

I read incoming envelopes once per weekday morning. "Now" urgency is reserved
for emergencies — use the override channel instead.

— signed by `{{SIGNER}}`
